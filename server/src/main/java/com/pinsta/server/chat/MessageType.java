package com.pinsta.server.chat;

// enum is enumeration, are often used to represent a fixed set of values ex: days of week (SUNDAY, MONDAY, etc), provides group of named constant values in readable and safer format
public enum MessageType {
    CHAT, // -||- chat
    JOIN, // indicates some one has joined
    LEAVE // -||- leaved
}
